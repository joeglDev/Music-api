# A discogs clone like API written in hono

## Quickstart

Create an environment file at route with `touch .env` and populate it based on the file '.env.example'.

Run `yarn` to install dependencies and `yarn dev` to start the server in local development mode.

## Documentation

Run `yarn docs` to update the openAPI schema.
